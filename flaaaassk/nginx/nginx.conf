server {
    listen 80;
    server_name _;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/uwsgi.sock;
    }

    location /static/ {
        alias /app/static/;
        expires 30d;
        access_log off;
    }
}